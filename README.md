Instalacion de librerias para debian/ubuntu:

$ sudo apt install python3-pip python3-virtualenv python3-venv

$ pip install pandas


Instalacion para Termux:

$ pkg update && pkg upgrade

$ pkg install python

$ pip install pandas 
